export default {
  // Enable class-based dark mode so adding the `dark` class to the <html>
  // element toggles dark variants (e.g. `dark:bg-slate-900`).
  darkMode: 'class',
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
